all:
    hosts:
        prod-server:
            ansible_host: nocost
            ansible_user: root
            ansible_port: 22
            ansible_ssh_private_key_file: ~/.ssh/id_rsa

    vars:
        env: prod
