check filesystem rootfs with path /
  if space usage > 80% then alert
  if inode usage > 80% then alert

check filesystem backup with path {{ paths.backup_root }}
  if space usage > 80% then alert
  if inode usage > 80% then alert